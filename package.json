{
   "name": "coffee-pot-monitor",
   "version": "0.0.1",
   "description": "IoT coffee pot monitoring system monorepo",
   "author": "Ethan Smith",
   "license": "GPL-3.0",
   "private": true,
   "workspaces": [
      "api",
      "packages/*",
      "web"
   ],
   "scripts": {
      "check": "npm run check --workspace=packages/schemas && npm run check --workspace=api && npm run check --workspace=web",
      "format": "prettier --write \"**/*.{js,ts,vue,json,md,css,scss,html}\"",
      "format:check": "prettier --check \"**/*.{js,ts,vue,json,md,css,scss,html}\"",
      "standards": "npm run format:check && npm run check"
   },
   "devDependencies": {
      "esbuild": "0.25.9",
      "prettier": "3.6.2",
      "prettier-plugin-svelte": "3.4.0",
      "typescript": "5.9.2"
   },
   "dependencies": {
      "jose": "6.0.12",
      "zod": "4.0.15"
   }
}
